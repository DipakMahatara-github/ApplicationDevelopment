@page "/edit-entry/{Id:guid}"
@using ApplicationDevelopment.Model
@using ApplicationDevelopment.Service
@inject NavigationManager NavManager

<div class="entry-page">
    <div class="entry-card shadow-sm">
        <div class="card-header">
            <h2>Edit Journal Entry</h2>
        </div>

        @if (_entryToEdit != null)
        {
            <EditForm Model="@_entryToEdit" OnValidSubmit="HandleUpdateAsync">
                <DataAnnotationsValidator />
                <ValidationSummary class="text-danger mb-2" />

                <div class="form-row">
                    <div class="form-group">
                        <label>Primary Mood</label>
                        <InputSelect @bind-Value="_entryToEdit.PrimaryMood" class="mood-select primary">
                            <option value="">-- Select --</option>
                            <option value="Happy">Happy</option>
                            <option value="Calm">Calm</option>
                            <option value="Sad">Sad</option>
                            <option value="Angry">Angry</option>
                        </InputSelect>
                    </div>

                    <div class="form-group">
                        <label>Secondary Mood</label>
                        <InputSelect @bind-Value="_entryToEdit.SecondaryMood" class="mood-select secondary">
                            <option value="">None</option>
                            <option value="Excited">Excited</option>
                            <option value="Tired">Tired</option>
                            <option value="Grateful">Grateful</option>
                        </InputSelect>
                    </div>
                </div>

                <div class="form-group tags-group">
                    <label>Edit Tags</label>
                    <div class="tag-input">
                        <InputText @bind-Value="_entryToEdit.CurrentTagInput" placeholder="Add new tag" />
                        <button type="button" class="btn btn-add" @onclick="AddTag">Add</button>
                    </div>
                    <div class="tag-list">
                        @foreach (var tag in _entryToEdit.Tags)
                        {
                            <span class="tag-badge">@tag <span class="remove-tag" @onclick="() => RemoveTag(tag)">×</span></span>
                        }
                    </div>
                </div>

                <div class="form-group">
                    <label>Journal Content</label>
                    <InputTextArea @bind-Value="_entryToEdit.Content" class="content-area" />
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn btn-save">Update Entry</button>
                    <button type="button" class="btn btn-cancel" @onclick="CancelAsync">Cancel</button>
                </div>
            </EditForm>
        }
        else
        {
            <p class="text-center text-muted p-4">Entry not found.</p>
        }
    </div>
</div>

<style>
.entry-page {
    display: flex;
    justify-content: center;
    margin: 2rem auto;
}

.entry-card {
    width: 100%;
    max-width: 700px;
    background: #fefefe;
    border-radius: 1rem;
    overflow: hidden;
}

.card-header {
    background: linear-gradient(120deg,#5D3AEE,#A56EFF); /* MATCH AddEntry */
    color: white;
    padding: 1.5rem;
    text-align: center;
    font-weight: 600;
}

.form-row {
    display: flex;
    gap: 1rem;
    padding: 1rem;
}

.form-group {
    flex: 1;
    display: flex;
    flex-direction: column;
    margin-bottom: 1rem;
}

label {
    margin-bottom: 0.5rem;
    font-weight: 600;
    color: #4a3f9d;
}

.mood-select {
    padding: 0.5rem;
    border-radius: 0.5rem;
    border: 1px solid #ccc;
}

.tags-group {
    padding: 0 1rem;
}

.tag-input {
    display: flex;
    gap: 0.5rem;
}

.btn-add {
    background: #5D3AEE; /* MATCH AddEntry */
    color: white;
    border: none;
    border-radius: 0.5rem;
    padding: 0.4rem 0.8rem;
}

.tag-list {
    margin-top: 0.5rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.4rem;
}

.tag-badge {
    background: #dcd7f7; /* MATCH AddEntry */
    color: #5D3AEE; /* MATCH AddEntry */
    padding: 0.3rem 0.7rem;
    border-radius: 1rem;
    font-size: 0.85rem;
}

.remove-tag {
    cursor: pointer;
    margin-left: 0.3rem;
    font-weight: 600;
    color: #fff;
}

.content-area {
    width: 100%;
    min-height: 200px;
    border-radius: 0.75rem;
    border: 1px solid #ccc;
    padding: 0.75rem;
    font-size: 1rem;
    resize: vertical;
    box-shadow: inset 0 1px 3px rgba(0,0,0,0.05);
}

.form-actions {
    padding: 1rem;
    text-align: center;
}

.btn-save {
    background: linear-gradient(120deg,#5D3AEE,#A56EFF); /* MATCH AddEntry */
    color: white;
    border: none;
    padding: 0.6rem 1.5rem;
    border-radius: 0.75rem;
    font-weight: 600;
    font-size: 1rem;
}

.btn-save:hover {
    opacity: 0.9;
}

.btn-cancel {
    background: #ccc;
    color: #333;
    padding: 0.6rem 1.5rem;
    border-radius: 0.75rem;
    margin-left: 1rem;
}
</style>

@code {
    [Parameter] public Guid Id { get; set; }
    private JournalEntry? _entryToEdit;

    protected override void OnInitialized()
    {
        _entryToEdit = JournalService.GetEntryById(Id);
    }

    private void AddTag()
    {
        if (!string.IsNullOrWhiteSpace(_entryToEdit?.CurrentTagInput))
        {
            _entryToEdit.Tags.Add(_entryToEdit.CurrentTagInput);
            _entryToEdit.CurrentTagInput = "";
        }
    }

    private void RemoveTag(string tag)
    {
        _entryToEdit?.Tags.Remove(tag);
    }

    private void HandleUpdateAsync()
    {
        NavManager.NavigateTo("/history");
    }

    private void CancelAsync()
    {
        NavManager.NavigateTo("/history");
    }
}
