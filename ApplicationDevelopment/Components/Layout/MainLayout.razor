@inherits LayoutComponentBase
@using ApplicationDevelopment.Service

<div class="app-container">
    <div class="vertical-navbar">
        <h4 class="nav-title">My Journal</h4>

        <a href="/add-entry" class="nav-link @(AuthService.IsUnlocked() ? "" : "disabled-link")">Add Entry</a>
        <a href="/history" class="nav-link @(AuthService.IsUnlocked() ? "" : "disabled-link")">History</a>
        <a href="/lock" class="nav-link">Unlock</a>
        <a href="/register" class="nav-link">Set PIN</a>
    </div>

    <div class="main-content">
        @Body
    </div>
</div>

<style>
    .disabled-link {
        pointer-events: none;
        opacity: 0.4;
    }
</style>
